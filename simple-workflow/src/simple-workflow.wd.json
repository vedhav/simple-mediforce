{
  "name": "simple-workflow",
  "title": "Simple Workflow",
  "description": "Pick one ClinicalTrials.gov NCT id from a fixed list of five studies. Single human step; the run completes on submit.",
  "visibility": "private",
  "triggers": [
    { "type": "manual", "name": "manual" }
  ],
  "roles": ["operator"],
  "steps": [
    {
      "id": "select-study",
      "name": "Select Study",
      "type": "creation",
      "executor": "human",
      "allowedRoles": ["operator"],
      "description": "Choose the NCT id of the study this run works on.",
      "params": [
        {
          "name": "nctId",
          "type": "string",
          "required": true,
          "description": "ClinicalTrials.gov registry identifier of the selected study",
          "options": [
            "NCT02511184",
            "NCT02563548",
            "NCT02573259",
            "NCT04672460",
            "NCT04822298"
          ]
        }
      ]
    },
    {
      "id": "done",
      "name": "Done",
      "type": "terminal",
      "executor": "human"
    }
  ],
  "transitions": [
    { "from": "select-study", "to": "done" }
  ]
}
